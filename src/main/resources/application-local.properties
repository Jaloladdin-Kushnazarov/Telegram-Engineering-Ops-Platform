# ===================================================================
# Local dev profili — Docker Compose dagi PostgreSQL uchun
# ===================================================================
# Datasource sozlamalari base application.properties'dan inherit qilinadi.
# Default qiymatlar docker-compose.yml ga mos keladi (engops/engops/engops_local).
# Override kerak bo'lsa DATABASE_URL / DATABASE_USERNAME / DATABASE_PASSWORD
# env vars'ni o'rnating.

# SQL so'rovlarni konsolda ko'rsatadi — debug uchun qulay
spring.jpa.show-sql=true

logging.level.com.engops.platform=DEBUG
logging.level.org.hibernate.SQL=DEBUG